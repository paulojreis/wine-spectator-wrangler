# Wine Spectator's Top 100 Wrangler.

This is still WIP. Stay tuned for cool stuff: *wine & data* - what could go wrong? :)

Run with `node main.js` to execute the script; wrangling results will be stored in `*.json` files.